name: purge
loop: false
focus_mode: exit
await_focus: true
steps:
  - wait_template: my_car_menu
  - press: "y"
  - repeat:
      key: down
      times: 2
  - press: enter
  - press: escape
  # Find and purge
  - scroll_to_target:
  - purge_duplicates:
